class queryTree:
    def __init__(self, renames, joins):
        print('\n')
        for statement in joins:
            stat = str(statement)
            temp = [x.strip() for x in stat.split("=")]
            table_name =
            print("stat", splits[0])

        #self.relations = []